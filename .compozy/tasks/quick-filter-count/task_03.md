---
status: completed
title: "Web — GraphQL operation + codegen"
type: web
complexity: low
dependencies:
  - task_02
---

# Task 03: Web — GraphQL operation + codegen

## Overview

Create the `quick-filter-counts.graphql` document with the new query, run codegen to generate the `useQuickFilterCountsQuery` hook, and verify it compiles correctly.

<critical>
- ALWAYS READ the PRD and TechSpec before starting
- REFERENCE TECHSPEC "System Architecture" section for the data flow
- FOCUS ON "WHAT" — describe what needs to be accomplished, not how
- MINIMIZE CODE — show code only to illustrate current structure or problem areas
- TESTS REQUIRED — every task MUST include tests in deliverables
</critical>

<requirements>
- MUST create `apps/web/src/graphql/quick-filter-counts.graphql` with the `QuickFilterCounts` query
- MUST request `key` and `count` fields with optional `$company` and `$runId` variables
- MUST run `pnpm --filter @job-tracker/web run codegen`
- MUST NOT hand-edit any generated files in `src/gql/`
- MUST verify `useQuickFilterCountsQuery` hook exists in generated code
</requirements>

## Subtasks

- [x] 3.1 Create `quick-filter-counts.graphql` with the query definition
- [x] 3.2 Run codegen: `pnpm --filter @job-tracker/web run codegen`
- [x] 3.3 Verify the generated hook exists and compiles
- [x] 3.4 Run `pnpm typecheck` to confirm no errors

## Implementation Details

New file:

- `apps/web/src/graphql/quick-filter-counts.graphql`

```graphql
query QuickFilterCounts($company: String, $runId: ID) {
  quickFilterCounts(company: $company, runId: $runId) {
    key
    count
  }
}
```

### Relevant Files

- `apps/web/src/graphql/quick-filter-counts.graphql` — New file
- `apps/web/src/gql/hooks.ts` — Regenerated by codegen

### Dependent Files

- `apps/web/src/modules/jobs/list/components/QuickFilters.tsx` — Consumer in task 04

### Related ADRs

- [ADR-002: Separate GraphQL Query for Filter Counts](../adrs/adr-002.md)

## Deliverables

- `apps/web/src/graphql/quick-filter-counts.graphql` — New file
- `apps/web/src/gql/` — Regenerated via codegen

## Tests

- No manual tests needed (codegen generates types that are validated by typecheck)
- [ ] `pnpm typecheck` passes
- [ ] `useQuickFilterCountsQuery` is exported from generated hooks

## Success Criteria

- Codegen completes without errors
- `pnpm typecheck` passes
- `useQuickFilterCountsQuery` hook is available
