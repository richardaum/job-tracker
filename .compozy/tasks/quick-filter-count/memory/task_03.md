# Task Memory: task_03.md

## Objective Snapshot

Create `quick-filter-counts.graphql` operation, run codegen, and verify the generated hook compiles.

## Important Decisions

- Query operation name: `QuickFilterCounts` — matches the GraphQL query name from the schema.
- Variables: `$company: String`, `$runId: ID` — matches the resolver signature from task_02.

## Learnings

- Codegen regenerates `src/gql/hooks.ts`, `src/gql/sdk.ts`, and `src/gql/` types.
- The post-processing script (`postprocess-codegen-hooks.mjs`) runs automatically after codegen.

## Files / Surfaces

- `apps/web/src/graphql/quick-filter-counts.graphql` — Created
- `apps/web/src/gql/hooks.ts` — Regenerated by codegen

## Errors / Corrections

None.

## Ready for Next Run

Yes — task_04 (Web — QuickFilters + mutation refetch).
