# Task Memory: task_05.md

## Objective Snapshot

Add `isJobDuplicate` GraphQL operation file, run codegen, wire `ApiService.isJobDuplicate()`.

## Important Decisions

- Temporarily added `isJobDuplicate` to `schema.gql` manually because `source-api` cannot restart (pre-existing migration issue with `CreatePlansTable1768010000000`). Will be regenerated when API starts successfully.

## Files / Surfaces

- `apps/extension/src/graphql/is-job-duplicate.graphql` — new query
- `apps/extension/src/gql/graphql.ts` — codegen output (auto)
- `apps/extension/src/gql/gql.ts` — codegen output (auto)
- `apps/extension/src/domains/api/api.service.ts` — added `isJobDuplicate()` method
- `apps/extension/src/domains/api/api.service.test.ts` — unit tests
- `apps/api/src/schema.gql` — temporary edit (will be regenerated by API)

## Errors / Corrections

- `source-api` PM2 process has pre-existing migration failure (`relation "plans" already exists`). Had to manually patch schema.gql for codegen.
- `oxlint` not installed in worktree env — lint fails for whole extension package, not task-specific.

## Ready for Next Run

Yes.
