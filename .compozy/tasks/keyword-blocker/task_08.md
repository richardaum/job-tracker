---
status: completed
title: "Codegen"
type: chore
complexity: low
dependencies: [task_01, task_05]
---

# Task 08: Codegen

## Overview

Run `pm2 restart api` to regenerate `schema.gql` with the new types and enum members from tasks 01 and 05, then run `pnpm --filter @job-tracker/web run codegen` to regenerate the frontend GraphQL hooks. Verify that the generated types, enums, and hooks are correct and buildable.

<critical>
- Read AGENTS.md § Generated files rules
- Reference TechSpec § "Build Order" step 8
- Do NOT hand-edit generated artifacts
- Verify with typecheck after codegen
</critical>

<requirements>
- MUST run `pm2 restart api` to regenerate `schema.gql`
- MUST verify `schema.gql` contains `BlockedKeyword`, `KeywordScope`, `MatchMode`, `ApplicationQuickFilter.REJECTED`
- MUST run `pnpm --filter @job-tracker/web run codegen`
- MUST verify `apps/web/src/gql/` has new types and hooks for `settings` query with `blockedKeywords` and `blockedCompanies`
- MUST run typecheck on both API and web to confirm no type errors
- MUST NOT hand-edit any generated files
</requirements>

## Subtasks

- [ ] Run `pm2 restart api`
- [ ] Verify `schema.gql` contains new types
- [ ] Run `pnpm --filter @job-tracker/web run codegen`
- [ ] Verify generated files in `apps/web/src/gql/`
- [ ] Run `pnpm typecheck` across affected apps

## Implementation Details

- **PM2**: `pm2 restart api` (or slug-version: `pm2 restart keywords-api` for worktree)
- **Codegen**: `pnpm --filter @job-tracker/web run codegen`
- **Verification**: check `apps/api/src/schema.gql` for `BlockedKeyword`, `KeywordScope`, `MatchMode`, `ApplicationQuickFilter.REJECTED`

### Relevant Files

| File                        | Reason                     |
| --------------------------- | -------------------------- |
| `apps/api/src/schema.gql`   | Regenerated by PM2 restart |
| `apps/web/src/gql/hooks.ts` | Regenerated by codegen     |
| `apps/web/src/gql/types.ts` | Regenerated by codegen     |

### Dependent Files

| File                                                             | Reason                             |
| ---------------------------------------------------------------- | ---------------------------------- |
| `apps/web/src/modules/profile/settings/page/SettingsTabPage.tsx` | Consumes generated hooks (task 06) |

### Related ADRs

- None

## Deliverables

- Regenerated `schema.gql`
- Regenerated frontend GraphQL hooks and types
- Passing typecheck

## Tests

### Verification

- [ ] `schema.gql` contains `type BlockedKeyword` with `keyword`, `scope`, `matchMode` fields
- [ ] `schema.gql` contains `input BlockedKeywordInput` with same fields
- [ ] `schema.gql` contains `enum KeywordScope` with `TITLE`, `DESCRIPTION`, `COMPANY`
- [ ] `schema.gql` contains `enum MatchMode` with `PARTIAL`, `EXACT`
- [ ] `schema.gql` contains `REJECTED` in `ApplicationQuickFilter` enum
- [ ] `schema.gql` contains `UserSetting.blockedKeywords` and `UserSetting.blockedCompanies`
- [ ] Generated hooks include `useSettingsQuery` returning new fields
- [ ] `pnpm typecheck` passes for API and web

## Success Criteria

- `schema.gql` includes all new types and enum members
- Frontend codegen produces valid hooks and types
- `pnpm typecheck` passes with zero errors
- No hand-edits to generated files
