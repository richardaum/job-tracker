# Workflow Memory

Keep only durable, cross-task context here. Do not duplicate facts that are obvious from the repository, PRD documents, or git history.

## Current State

## Shared Decisions

## Shared Learnings

## Open Risks

- `pnpm validate:ci` currently stops at the repository-wide `knip` gate on a pre-existing dead-code backlog (unused files/dependencies/exports unrelated to AI usage); project rules require user approval before removing it.

## Handoffs
