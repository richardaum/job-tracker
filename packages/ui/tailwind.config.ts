/**
 * Tailwind CSS v4 configuration.
 *
 * In v4, the primary configuration is done in CSS via `@import "tailwindcss"`
 * and `@theme` blocks in globals.css. This file is kept for tooling
 * compatibility and documents the content paths used by the package.
 */

export default { content: ["./src/**/*.{ts,tsx}"], theme: { extend: {} } };
