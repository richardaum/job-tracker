query AuthenticatedShell {
  me {
    id
    email
    name
    role
    avatarUrl
    accounts {
      id
      providerName
      providerAccountId
      createdAt
    }
  }
  settings {
    id
    autoFillEnabled
    autoSummaryEnabled
    autoMatchEnabled
    duplicateWindowDays
  }
}
