query AuthenticatedShell {
  me {
    id
    email
    name
    role
    avatarUrl
    authProviders
  }
  settings {
    id
    autoFillEnabled
    autoSummaryEnabled
    autoMatchEnabled
    duplicateWindowDays
  }
}
