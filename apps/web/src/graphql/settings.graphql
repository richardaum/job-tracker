query Settings {
  settings {
    id
    autoFillEnabled
    autoSummaryEnabled
    autoMatchEnabled
    duplicateWindowDays
  }
}

mutation UpdateSettings($input: UpdateSettingsInput!) {
  updateSettings(input: $input) {
    id
    autoFillEnabled
    autoSummaryEnabled
    autoMatchEnabled
    duplicateWindowDays
  }
}
