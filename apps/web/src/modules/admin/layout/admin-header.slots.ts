import { PortalSlot } from "@job-tracker/react-slots";

export const AdminHeaderActions = PortalSlot("admin-header-actions");

/** Tab-owned controls (e.g. status filters) rendered beside the primary admin tab bar. */
export const AdminSubtabsSlot = PortalSlot("admin-tab-bar-extras");
